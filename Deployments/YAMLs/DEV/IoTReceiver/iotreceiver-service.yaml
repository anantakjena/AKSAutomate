apiVersion: v1
kind: Service
metadata:
  name: iotreceiver
  namespace: aks-workshop-dev
spec:
  selector:
    app: iotreceiver
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP